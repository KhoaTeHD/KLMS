@page
@model ForgotPasswordModel
@{
    ViewData["Title"] = "Quên mật khẩu";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="card shadow-lg border-0">
                <div class="card-body p-5">
                    <div class="text-center mb-4">
                        <div class="mb-3">
                            <i class="bi bi-key text-primary" style="font-size: 3rem;"></i>
                        </div>
                        <h2 class="fw-bold text-primary">@ViewData["Title"]</h2>
                        <p class="text-muted">Nhập email của bạn để nhận liên kết khôi phục mật khẩu</p>
                    </div>

                    <form method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>

                        <div class="mb-4">
                            <label asp-for="Input.Email" class="form-label">
                                <i class="bi bi-envelope me-1"></i>Địa chỉ Email
                            </label>
                            <input asp-for="Input.Email" class="form-control form-control-lg"
                                   autocomplete="username" aria-required="true" placeholder="name@example.com" />
                            <span asp-validation-for="Input.Email" class="text-danger"></span>
                            <div class="form-text">
                                Chúng tôi sẽ gửi liên kết khôi phục mật khẩu đến email này
                            </div>
                        </div>

                        <div class="d-grid mb-4">
                            <button type="submit" class="btn btn-primary btn-lg">
                                <i class="bi bi-send me-2"></i>Gửi liên kết khôi phục
                            </button>
                        </div>
                    </form>

                    <div class="text-center">
                        <div class="row g-3">
                            <div class="col-sm-6">
                                <a asp-page="./Login" class="btn btn-outline-secondary w-100">
                                    <i class="bi bi-arrow-left me-2"></i>Quay lại đăng nhập
                                </a>
                            </div>
                            <div class="col-sm-6">
                                <a asp-page="./Register" class="btn btn-outline-primary w-100">
                                    <i class="bi bi-person-plus me-2"></i>Tạo tài khoản mới
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Help Information -->
            <div class="card shadow-sm mt-4">
                <div class="card-header bg-light">
                    <h6 class="mb-0">
                        <i class="bi bi-info-circle me-2"></i>Hướng dẫn khôi phục mật khẩu
                    </h6>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-6">
                            <h6 class="text-primary">Bước 1: Nhập email</h6>
                            <p class="small">Nhập đúng địa chỉ email bạn đã đăng ký tài khoản</p>
                        </div>
                        <div class="col-md-6">
                            <h6 class="text-primary">Bước 2: Kiểm tra email</h6>
                            <p class="small">Kiểm tra hộp thư đến (và thư mục spam) để tìm email khôi phục</p>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-6">
                            <h6 class="text-primary">Bước 3: Nhấn liên kết</h6>
                            <p class="small">Nhấn vào liên kết trong email để thiết lập mật khẩu mới</p>
                        </div>
                        <div class="col-md-6">
                            <h6 class="text-primary">Bước 4: Tạo mật khẩu mới</h6>
                            <p class="small">Nhập mật khẩu mới và hoàn tất quá trình khôi phục</p>
                        </div>
                    </div>
                    <div class="alert alert-warning mt-3">
                        <small>
                            <i class="bi bi-exclamation-triangle me-1"></i>
                            <strong>Lưu ý:</strong> Liên kết khôi phục chỉ có hiệu lực trong 24 giờ.
                            Nếu không nhận được email, vui lòng liên hệ với quản trị viên.
                        </small>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

@section Styles {
    <style>
        .card {
            border-radius: 15px;
        }

        .form-control-lg {
            padding: 0.75rem 1rem;
            border-radius: 8px;
        }

        .btn-lg {
            padding: 0.75rem 1.5rem;
            border-radius: 8px;
        }
    </style>
}